export const initialState = {
    activities: [],
    loading: false,
    hasErrors: false,
}

export default function activitiesReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}