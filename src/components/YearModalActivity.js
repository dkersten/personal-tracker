import React from 'react'

// import styled from 'styled-components'

export const YearModalActivity = (props) => {

    return (
        <div>
            All activities from the month
        </div>
    )
}